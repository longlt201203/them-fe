import React from 'react';

import Action from './Action';

const Send = () => {
    return (
        <div>
            <Action className="bi bi-send" />
        </div>
    );
};

export default Send;
